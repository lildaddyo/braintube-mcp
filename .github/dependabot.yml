version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
    ignore:
      # Keep major version upgrades manual — only auto-bump patch/minor
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
